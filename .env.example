# ===== LARK-MAIL 环境变量示例 =====
# 使用方法:复制本文件为 .env,填写真实值后启动:
#   cp .env.example .env
#   docker compose up -d

# ---- 邮箱(IMAP)配置 ----
IMAP_SERVER=imap.163.com
IMAP_PORT=993
IMAP_USER=your_email@163.com
IMAP_PASS=your_auth_code            # 授权码,不是登录密码!
MAILBOX=INBOX

# ---- 飞书通知 ----
FEISHU_WEBHOOKS=https://open.feishu.cn/open-apis/bot/v2/hook/your_webhook

# ---- 运行参数(可选,使用默认值可注释掉) ----
POLL_INTERVAL=60                     # 轮询间隔(秒)
BLOCK_BEFORE=86400                   # 跳过多少秒之前的邮件,默认 24 小时
MAX_EMAILS_PER_RUN=50                # 每次最多处理邮件数
BATCH_DELAY=2                        # 每封邮件之间延时(秒),防止飞书限流
DEBUG=false                          # true 开启调试模式
